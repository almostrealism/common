# AR Consultant MCP Server dependencies
#
# Core MCP framework
# Held below 2.0.0: the 2.x SDK drops the 1.x server API this server
# imports (mcp.server.fastmcp / mcp.server.Server). Raising the bound
# requires migrating the server to the 2.x API first.
mcp>=1.0.0,<2

# Memory integration (shared with ar-memory)
fastembed>=0.2.0
faiss-cpu>=1.7.0
numpy

# Optional: MLX inference backend (Apple Silicon only)
# Uncomment on macOS with Apple Silicon:
# mlx>=0.4.0
# mlx-lm>=0.4.0
