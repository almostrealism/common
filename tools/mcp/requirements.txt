# Consolidated requirements for all AR MCP servers
# Install with: pip install -r tools/mcp/requirements.txt
#
# Covers: ar-consultant, ar-memory, ar-test-runner, ar-jmx,
#          ar-profile-analyzer, ar-github, ar-slack

# Core MCP framework (all servers)
mcp>=1.0.0

# Embeddings and vector search (ar-memory)
fastembed>=0.2.0
faiss-cpu>=1.7.0
numpy

# HTTP server (ar-memory HTTP API)
starlette>=0.27.0
uvicorn>=0.23.0

# Optional: MLX inference backend (Apple Silicon only)
# Uncomment on macOS with Apple Silicon for local LLM in ar-consultant:
# mlx>=0.4.0
# mlx-lm>=0.4.0

# Optional: Alternative embedding backend (ar-memory)
# PyTorch-based, larger install (~1-2 GB). Use fastembed by default.
# Uncomment if you need the sentence-transformers backend:
# sentence-transformers
