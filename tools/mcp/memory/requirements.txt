# Held below 2.0.0: the 2.x SDK drops the 1.x server API this server
# imports (mcp.server.fastmcp / mcp.server.Server). Raising the bound
# requires migrating the server to the 2.x API first.
mcp>=1.0.0,<2
fastembed>=0.2.0
faiss-cpu>=1.7.0
numpy
starlette>=0.27.0
uvicorn>=0.23.0
