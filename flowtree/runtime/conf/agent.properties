# FlowTree Agent defaults
# The agent connects OUT to the controller via FLOWTREE_ROOT_HOST/PORT env vars.
# See NodeGroup.startPersistentHost() for the auto-reconnect mechanism.

server.port=-1
nodes.peers.max=4
nodes.jobs.max=1
group.msc=30
network.msg.verbose=false
network.msg.dverbose=true
